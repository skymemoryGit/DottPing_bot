# --- core ---
python-telegram-bot[job-queue,rate-limiter]>=21.6,<23
httpx>=0.27
beautifulsoup4>=4.12
python-dotenv>=1.0

# --- opzionale ---
# Ripiego se il portale filtra le richieste dall'IP del server (tipico sui VPS):
# replica il TLS fingerprint di Chrome.
curl-cffi>=0.7 ; python_version >= "3.9"
